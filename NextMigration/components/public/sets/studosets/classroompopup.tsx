interface ClassroomPopupProps {
    toggled: boolean;
    setPopUpToggle:React.Dispatch<React.SetStateAction<boolean>>;
}
export default function ClassroomPopup({toggled, setPopUpToggle}: ClassroomPopupProps) {
    return(<div></div>)
}
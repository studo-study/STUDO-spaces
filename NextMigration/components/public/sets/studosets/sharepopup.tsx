interface SharePopupProps {
    toggled: boolean;
    setPopUpToggle:React.Dispatch<React.SetStateAction<boolean>>;
}
export default function SharePopup({toggled, setPopUpToggle}: SharePopupProps) {
    return(<div></div>)
}